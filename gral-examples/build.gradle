description = 'GRAL example applications'

dependencies {
	compile project(':gral-core')
}

distributions {
	main {
		contents {
			from(projectDir) {
				include("src/**")
			}
			into("lib") {
				from jar
				from configurations.runtime
			}
		}
	}
}