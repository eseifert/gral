description = "GRAL example applications"

dependencies {
	compile project(":gral-core")
	compile project(":gral-uml")
	compile(group: 'metamodel', name: 'metamodel-examples', version: '0.0+')
}

distributions {
	standard {
		contents {
			from(projectDir) {
				include("src/**")
			}
			into("lib") {
				from jar
				from configurations.runtime
			}
		}
	}
}

jar {
	manifest {
		attributes("Main-Class": "de.erichseifert.gral.examples.Browser")
	}
}