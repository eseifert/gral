sudo: false

language: java

jdk:
  - oraclejdk7
  - openjdk6

notifications:
  irc:
    channels:
      - "irc.freenode.org#gral"
    on_success: change
    on_failure: always
    template:
      - "%{repository} (%{branch}:%{commit} by %{author}): %{message} (%{build_url})"
    skip_join: true
